{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

    <div class="jumbotron jumbotron-transparent">
        <div class="container">
            <div class="row">
                <div class="col-md-5">
                    <h2>Ha full kontroll på driften. Alltid. </h2>
                    <p>
                        Logg inn hvor som helst i verden, når som helst, på Mac, PC, nettbrett eller telefon og ha
                        full oversikt over hele taxidriften din. Det er administrasjon gjort enkelt.
                    </p>
                </div>
                <div class="col-md-offset-2 col-md-4">
                    <ul class="nav nav-tabs nav-justified nav-tabs-taxigo" role="tablist">
                        <li role="presentation">
                            <a href="{{ path('login') }}" aria-controls="login" role="tab" data-toggle="tab">Log in</a>
                        </li>
                        <li role="presentation" class="active">
                            <a href="#profile" aria-controls="profile" role="tab" data-toggle="tab">Not registered?</a>
                        </li>
                    </ul>

                    {% if errors %}
                        <div class="row">
                            <div class="alert">{{ dump(errors) }}</div>
                            {% for error in errors %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </div>
                    {% endif %}

                    <div class="tab-content">
                        <div role="tabpanel" class="tab-pane" id="home">

                        </div>
                        <div role="tabpanel" class="tab-pane active" id="profile">
                            {{ form_start(form) }}
                            <div class="form-group">
                                {{ form_label(form.username, null, {'label_attr': {'class': 'sr-only' }}) }}
                                <div class="">
                                    {{ form_widget(form.username, {'attr': {'placeholder': 'username', 'class': 'form-control' }}) }}
                                </div>
                            </div>
                            <div class="form-group">
                                {{ form_label(form.email, null, {'label_attr': {'class': 'sr-only' }}) }}
                                <div class="">
                                    {{ form_widget(form.email, {'attr': {'placeholder': 'email', 'class': 'form-control' }}) }}
                                </div>
                            </div>
                            <div class="form-group">
                                {{ form_label(form.plainPassword.first, null, {'label_attr': {'class': 'sr-only' }}) }}
                                <div class="">
                                    {{ form_widget(form.plainPassword.first, {'attr': {'placeholder': 'password', 'class': 'form-control' }}) }}
                                </div>
                            </div>
                            <div class="form-group">
                                {{ form_label(form.plainPassword.second, null, {'label_attr': {'class': 'sr-only' }}) }}
                                <div class="">
                                    {{ form_widget(form.plainPassword.second, {'attr': {'placeholder': 'repeat password', 'class': 'form-control' }}) }}
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="">
                                    <button type="submit" class="btn btn-block btn-success">Register</button>
                                </div>
                            </div>
                            {{ form_widget(form._token) }}
                            {{ form_end(form, {'render_rest': false }) }}
                        </div>
                    </div>

                </div>
            </div><!-- .row -->
        </div>
    </div><!-- .jumbotron -->

    <div class="container">
        <div class="row">
            <div class="col-md-7">
                <h4>Fordeler</h4>
            </div>
            <div class="col-md-4">
                <p>
                    Bruk et verktøy som er laget for å gjøre jobben din enklere, og som vil hjelpe deg i å øke lønnsomheten.
                    Taxigo er et sikkert og stabilt hjelpemiddel, og vi er alltid imøtekommende om det er noe du lurer på.
                </p>
            </div>
        </div>
    </div>

{% endblock %}